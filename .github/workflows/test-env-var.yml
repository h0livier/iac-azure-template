name: Test environment variable

on:
  workflow_dispatch:

jobs:
    test-env-var:
        runs-on: ubuntu-latest
        environment: dev
        steps:
        - name: Check environment variable
          run: |
            echo "The value of the environment variable is: ${{ vars.TEST_ENV }}"
            echo "here is the other: ${{ secrets.TEST }}"